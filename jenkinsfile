pipeline{
    agent any
    stages{
        stage("build"){
            steps{
             script{
                def env = System.getenv()
                env.each{
                println it
            } 
            }
        } 
 
    }
}
}