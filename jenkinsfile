pipeline{
    agent any
    stages{
        stage("build"){
            steps{
             script{
               branch = sh "echo ${env.BRANCH_NAME}"
               commithash = sh "echo ${env.GIT_COMMIT}"
               echo ${branch} 
               echo ${commithash}
            }
        } 
 
    }
}
}