pipeline{
    agent any
    stages{
        stage("build"){
            when {env.BRANCH_NAME == 'dev'}
            steps{
                script{
                sh "docker build -t app"
                sh "docker tag app echoapp:${env.GIT_COMMIT}"
                sh "echo tagged good"
                } 
 
            }
            }
        }
    }
}