pipeline{
    agent any
    stages{
        if(env.BRANCH_NAME == 'dev'){
        stage("build"){
            steps{
                script{
                sh "docker build -t app"
                sh "docker tag app echoapp:${env.GIT_COMMIT}"
                sh "echo tagged good"
                } 
 
            }
        }
        }
    }
}